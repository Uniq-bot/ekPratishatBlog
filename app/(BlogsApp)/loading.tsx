import PageLoader from "@/components/shared/PageLoader";

const BlogsLoading = () => {
  return <PageLoader title="Loading blogs" subtitle="Fetching articles, filters, and highlights." />;
};

export default BlogsLoading;